/*
var config = require('../js/configure.js'),
	mongoose = require('mongoose'),
	reqString = {
		type: String,
		required: true
	},
	Schema = new mongoose.Schema({
		_id: reqString,
		logchannel: {
			type: String,
			default: config.channel.log
		},
		nitro: {
			type: String,
			default: 'disable'
		}
	});
module.exports = mongoose.model('guild', Schema);
*/
